### Install and build library

```bash
cd gui4vega_react
npm install
npm run build
```

### Run demo

```bash
cd react_app
npm install
npm run dev
 ```

### Push to npm @relisa/gui4vega

```bash
cd gui4vega_react
npm run release:patch
# token to bump is required
```